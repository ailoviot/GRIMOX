# Hono Starter

Minimal Hono + TypeScript starter template.

## Setup

```bash
npm install
```

## Run

```bash
npm run dev
```

Server starts at http://localhost:3000

## Endpoints

- `GET /` — Welcome message
- `GET /api/health` — Health check
