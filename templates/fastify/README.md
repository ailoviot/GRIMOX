# Fastify Starter

Minimal Fastify + TypeScript starter template.

## Setup

```bash
npm install
```

## Run

```bash
npm run dev
```

Server starts at http://localhost:3000

## Endpoints

- `GET /` — Welcome message
- `GET /health` — Health check
- `GET /api/example` — Example endpoint
