# Expo Router Starter

React Native app with Expo Router and TypeScript.

## Getting Started

```bash
npm install
npm start
```

## Scripts

- `npm start` — Start Expo dev server
- `npm run android` — Run on Android
- `npm run ios` — Run on iOS
- `npm run web` — Run on web
